# UAlbertaALTLab Shh-dont-tell-brendan

## How do I install these formulae?
`brew install ualbertaaltlab/shh-dont-tell-brendan/<formula>`

Or `brew tap ualbertaaltlab/shh-dont-tell-brendan` and then `brew install <formula>`.

## Documentation
`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
